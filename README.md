this shouldn't be public but it's easier this way...

# usage

either `lein run` or `lein uberjar` & then run `target/relay.jar`,
it expects config path to be either `config.edn` or first argument. 

# license

GPLv3
